<template>
  <div class="loader">
    <div class="loader__wrapper"></div>
    <slot>
      <p v-if="showText" class="loader__text">جاري التحميل...</p>
    </slot>
  </div>
</template>
<script setup>
// ===== IMPORTS =====
// Vue and Libraries
// Stores
// Composables
// Components
// Constants
// Helpers
// ===== PROPS & EMITS =====
const props = defineProps({
  showText: {
    type: Boolean,
    default: true,
  },
});
// ===== STORE INSTANCES =====
// ===== COMPOSABLES =====
// ===== CONSTANTS =====
// ===== REACTIVE STATE & TEMPLATE REFS =====
// ===== VALIDATION RULES =====
// ===== COMPUTED PROPERTIES =====
// ===== LIFECYCLE HOOKS =====
// ===== METHODS =====
// ===== WATCHERS =====
// ===== EXPOSE PUBLIC METHODS =====
</script>
<style lang="scss" scoped>
.loader {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 24px;
  &__wrapper {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    display: inline-block;
    position: relative;
    border: 3px solid;
    border-color: #fff #fff transparent transparent;
    box-sizing: border-box;
    animation: rotation 1s linear infinite;
    &::after,
    &::before {
      content: "";
      box-sizing: border-box;
      position: absolute;
      left: 0;
      right: 0;
      top: 0;
      bottom: 0;
      margin: auto;
      border: 3px solid;
      border-color: transparent transparent var(--blue-500) var(--blue-500);
      width: 40px;
      height: 40px;
      border-radius: 50%;
      box-sizing: border-box;
      animation: rotationBack 0.5s linear infinite;
      transform-origin: center center;
    }
    &::before {
      width: 32px;
      height: 32px;
      border-color: #fff #fff transparent transparent;
      animation: rotation 1.5s linear infinite;
    }
  }
  &__text {
    color: var(--blue-500);
    font-size: 1.6rem;
    font-weight: 500;
  }
}

@keyframes rotation {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}
@keyframes rotationBack {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(-360deg);
  }
}
</style>
